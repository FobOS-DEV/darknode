services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vpn-telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    working_dir: /app
    volumes:
      - ./prisma:/app/prisma
